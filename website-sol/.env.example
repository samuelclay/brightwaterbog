# Drafts are shown automatically by `npm run dev` and `npm run build`.
PUBLIC_SHOW_DRAFTS=false

# Optional. Configure this only for the production Cloudflare build.
PUBLIC_CLOUDFLARE_ANALYTICS_TOKEN=
PUBLIC_SITE_URL=
